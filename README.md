# Inventory-and-Stock-Management
Inventory Management System (Spring Boot) — A robust backend REST API for managing products, users, and transactions.
Built using Spring Boot, Spring Security (JWT), Spring Data JPA, and MySQL, it supports role-based access, pagination,
filtering, and Swagger UI for API documentation.
